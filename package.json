{
  "name": "pitlimit-mlbb",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "premsg": "git branch && git add -A",
    "pramsg": "git push && git checkout main && git merge origin/developing && git push && git checkout developing && git branch",
    "vercel": "git branch && git add -A && git commit -m 'quickRun' && git push && git checkout main && git merge origin/developing && git push && git checkout developing && git branch"
  },
  "dependencies": {
    "@vercel/analytics": "^0.1.3",
    "bootstrap": "^5.2.2",
    "ismobilejs": "^1.1.1",
    "next": "13.0.1",
    "pg": "^8.8.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "eslint": "8.26.0",
    "eslint-config-next": "13.0.1"
  }
}
